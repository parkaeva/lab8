#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int a, b, c;
    cin>>a>>b>>c;
    if ((abs(b-a))<(abs(c-a)))
    {
      cout<<"B"<<endl<<abs(b-a);
    } else
    {
        cout<<"C"<<endl<<abs(c-a);
    }
    
    return 0;
}
